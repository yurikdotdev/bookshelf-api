{
  "name": "bookshelf-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "start:migrate": "bun prisma migrate deploy && bun run src/index.ts",
    "test:integration": "hurl --verbose --glob tests/*.hurl"
  },
  "dependencies": {
    "@prisma/client": "5.21.1",
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@orangeopensource/hurl": "^5.0.1",
    "@types/bun": "latest",
    "prisma": "^5.21.1"
  }
}
